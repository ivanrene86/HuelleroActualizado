export async function capturarHuella() {
  // TODO Fase 5: bucle de captura con DigitalPersona (dpfpdd.dll):
  // startSession / addCapture / completeEnrollment o captura simple de verificación.
  // Devuelve la imagen (base64) para que verify.js la compare.
  return null
}
